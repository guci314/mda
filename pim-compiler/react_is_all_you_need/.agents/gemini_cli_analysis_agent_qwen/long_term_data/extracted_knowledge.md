```markdown
# 知识库

## 元知识
- 通过任务执行历史分析，可提取与任务目标、交互模式相关的新经验
- 验证方法包括：确认用户意图、识别交互上下文、明确任务目标
- 新增：通过目录结构和文件名分析项目架构是一种有效的信息提取方式
- 新增：在理解方法执行逻辑时，优先查找相关实现文件（如核心模块目录），再结合上下文推断功能行为

## 原理与设计
- 任务执行助手的设计理念是通过自然语言交互理解用户需求并提供帮助
- 系统默认以友好、开放的方式响应初始问候，引导用户提供具体任务信息
- 新增：Gemini CLI 是一个命令行 AI 工作流工具，旨在连接工具、理解代码并加速工作流程
- 新增：Gemini CLI 支持查询和编辑大型代码库，突破模型上下文长度限制（如 1M token）
- 新增：`executeCommand` 方法作为命令执行入口，体现了模块化设计思想，将命令解析与执行逻辑解耦

## 接口与API
- 用户输入解析接口：接收自然语言指令并识别意图
- 响应生成接口：根据当前对话状态生成适当反馈（如问候响应）
- 新增工具使用记录：
  - `read_file`：用于读取特定文件内容（如 README.md、GEMINI.md）
  - `list_directory`：用于列出目录结构，辅助理解项目组织
- 新增：`executeCommand(command)` 是一个公共方法接口，用于触发命令执行流程

## 实现细节（需验证）
- 初始问候处理逻辑可能位于对话管理模块的启动流程中
- 响应生成可能依赖模板系统或预定义话术（如"你好！我是一个任务执行助手..."）
- 注：实现细节可能已变化，使用前需验证
- 新增：Gemini CLI 项目结构包含以下关键目录和文件（可能已变化）：
  - 核心模块可能位于 `cli/` 和 `core/` 目录中
  - 配置文件如 `.gemini/` 可能用于存储 CLI 状态或配置
  - 文档资源位于 `docs/` 目录中
  - 多轮对话状态保持机制可能在 `gemini_cli多轮对话保持状态.md` 中有描述
- 新增：`executeCommand` 方法的实现可能位于核心命令处理模块中，其执行逻辑包括命令解析、参数校验、调用具体处理器等步骤（需验证）

## 用户偏好与项目特点
- 用户初始交互模式为简单问候，尚未提供具体任务指令
- 系统需具备任务引导能力，帮助用户从开放对话过渡到具体任务执行
- 新增：用户对技术架构和核心组件的说明有明确需求
- 新增：用户关注方法级别的执行逻辑，并偏好自然语言而非代码形式的解释
```